APP_STL := c++_shared         # Use the shared version of the C++ Standard Library (STL)
APP_ABI := armeabi-v7a         # Target ARMv7 architecture (common for Android devices)
APP_PLATFORM := android-21     # Minimum Android API level (21 is a good balance for compatibility)
APP_OPTIM := release           # Build the shared library in release mode for optimization
